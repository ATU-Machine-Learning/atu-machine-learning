---
export interface NavLinkProps {
  href?: string;
  label: string;
}

const { href = "#", label } = Astro.props as NavLinkProps;
---

<li>
  <a href={href} class="btn"><span class="link-icon"><slot /></span>{label}</a>
</li>
